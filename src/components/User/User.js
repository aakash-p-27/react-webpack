import React from 'react';
import classes from './User.css';

const User = (props) =>(
    <div className={classes.User}>
        <h1>user is working properly</h1>
    </div>
);

export default User;
